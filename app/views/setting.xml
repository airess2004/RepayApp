<Alloy>
	<View class="container">
		<!--TitleControl>
    		<Label id="tab3Title">Setting</Label>
  		</TitleControl-->
		<ScrollView layout = "vertical">
			<View id="avatarView" onClick="imageClick">
				<ImageView id="image"></ImageView>
			</View>
			<Label id="email" text="username@repay.com"/>
			<Button id="signOutButton" onClick="signOutClick" title="Sign Out" ></Button>
			<Button id="syncButton" onClick="syncClick" title="Sync Now" ></Button>
		</ScrollView>
	</View>
</Alloy>
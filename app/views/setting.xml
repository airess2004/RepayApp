<Alloy>
	<View class="container">
		<!--TitleControl>
    		<Label id="tab3Title">Setting</Label>
  		</TitleControl-->
		<ScrollView layout = "vertical">
			<View id="avatarView" onClick="avatarClick">
				<ImageView id="avatar" />
			</View>
			<Label id="email" text="username@repay.com"/>
			<Button id="signOutButton" onClick="signOutClick" title="Log Out" ></Button>
			<Button id="syncButton" onClick="syncClick" title="Sync Now" ></Button>
		</ScrollView>
	</View>
</Alloy>